import {makeStyles} from "@fluentui/react-components";

const assetDashboardStyle = makeStyles({
    assetDashboard: {
        width: "calc(100vw - 403px)",
        height: 'calc(100vh - 49px) !important',
    }
})

export default assetDashboardStyle