import {makeStyles} from "@fluentui/react-components";

const masterDataStyle = makeStyles({
    masterData: {
        width: "calc(100vw - 403px)",
        height: "calc(100vh - 48px)",
    }
})

export default masterDataStyle